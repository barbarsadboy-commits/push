rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // EMERGENCY MODE: Allow everything
    // Use this ONLY for debugging "Missing or insufficient permissions"
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
